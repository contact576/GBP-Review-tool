// Vitest stub for the `server-only` package, which throws when imported in a
// non-RSC (plain Node) context. Aliased in vitest.config.ts so server modules
// with pure, testable helpers can be imported in unit tests.
export {};
